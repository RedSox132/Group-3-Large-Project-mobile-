import 'package:flutter/material.dart';

class AddRoutineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Routine')),
      body: Center(child: Text('Form to add a new routine.')),
    );
  }
}